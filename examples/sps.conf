# Basic SPS configuration. Paths below are relative to SPS_ROOT unless an
# environment variable overrides them.
db /var/lib/sps
cache /var/cache/sps
build /var/tmp/sps
makejobs 1
compression auto
preserve etc

# Leave arch unset to use `uname -m`.
